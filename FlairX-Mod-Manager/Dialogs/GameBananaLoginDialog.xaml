<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="FlairX_Mod_Manager.Dialogs.GameBananaLoginDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    PrimaryButtonText="Continue"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    CloseButtonClick="ContentDialog_CloseButtonClick">

    <StackPanel Spacing="12">

        <!-- Info -->
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <FontIcon Grid.Column="0" Glyph="&#xE946;" FontSize="16"
                          VerticalAlignment="Top"
                          Margin="0,2,10,0"/>
                <TextBlock Grid.Column="1"
                           x:Name="NsfwInfoText"
                           TextWrapping="Wrap"/>
            </Grid>
        </Border>

        <!-- Get Cookies button + status -->
        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button x:Name="GetCookiesButton" Click="GetCookiesButton_Click">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                    <TextBlock x:Name="GetCookiesButtonText" Text="Get Cookies"/>
                </StackPanel>
            </Button>
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Opacity="0.7"/>
        </StackPanel>

        <!-- WebView2 -->
        <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Height="420">
            <Grid x:Name="WebViewContainer"/>
        </Border>

        <!-- Don't ask again -->
        <CheckBox x:Name="DontAskAgainCheckBox"/>

    </StackPanel>
</ContentDialog>

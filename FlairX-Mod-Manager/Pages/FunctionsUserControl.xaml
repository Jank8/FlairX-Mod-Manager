<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FlairX_Mod_Manager.Pages.FunctionsUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlairX_Mod_Manager.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch">
    
    <Grid Margin="24" x:Name="MainGrid" Background="Transparent" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header with Back button -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0,0,0,24">
            <Button x:Name="BackButton" Width="48" Height="48" Click="BackButton_Click" VerticalAlignment="Center"
                    PointerEntered="BackButton_PointerEntered" PointerExited="BackButton_PointerExited">
                <FontIcon x:Name="BackIcon" Glyph="&#xE711;" FontSize="20">
                    <FontIcon.RenderTransform>
                        <ScaleTransform x:Name="BackIconScale" ScaleX="1" ScaleY="1"/>
                    </FontIcon.RenderTransform>
                </FontIcon>
            </Button>
            <TextBlock x:Name="FunctionsTitle" Text="Functions" FontSize="24" FontWeight="Bold" Margin="16,0,16,0" VerticalAlignment="Center"/>
        </StackPanel>
        
        <!-- Content with SelectorBar -->
        <Grid Grid.Row="1" Padding="0,0,0,24" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Centered SelectorBar -->
            <SelectorBar x:Name="FunctionSelectorBar" Grid.Row="0" SelectionChanged="FunctionSelectorBar_SelectionChanged" 
                         HorizontalAlignment="Center" Margin="0,0,0,16">
                <SelectorBarItem x:Name="GBAuthorUpdateItem" Text="GameBanana Update" IsSelected="True" Tag="GBAuthorUpdate">
                    <SelectorBarItem.Icon>
                        <FontIcon Glyph="&#xF49A;"/>
                    </SelectorBarItem.Icon>
                </SelectorBarItem>
                <SelectorBarItem x:Name="StatusKeeperItem" Text="Status Keeper" Tag="StatusKeeperPage">
                    <SelectorBarItem.Icon>
                        <FontIcon Glyph="&#xE792;"/>
                    </SelectorBarItem.Icon>
                </SelectorBarItem>
                <SelectorBarItem x:Name="ModInfoBackupItem" Text="ModInfo Backup" Tag="ModInfoBackup">
                    <SelectorBarItem.Icon>
                        <FontIcon Glyph="&#xE8C8;"/>
                    </SelectorBarItem.Icon>
                </SelectorBarItem>
                <SelectorBarItem x:Name="ImageOptimizerItem" Text="Image Optimizer" Tag="ImageOptimizer">
                    <SelectorBarItem.Icon>
                        <FontIcon Glyph="&#xE91B;"/>
                    </SelectorBarItem.Icon>
                </SelectorBarItem>
            </SelectorBar>
            
            <!-- Content Frame -->
            <Frame x:Name="FunctionContentFrame" Grid.Row="1" IsNavigationStackEnabled="False"/>
        </Grid>
    </Grid>
</UserControl>
<UserControl
    x:Class="FlairX_Mod_Manager.Pages.PresetsUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlairX_Mod_Manager.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch">
    
    <Grid Margin="24" x:Name="MainGrid" Background="Transparent" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header with Back button -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0,0,0,24">
            <Button x:Name="BackButton" Width="48" Height="48" Click="BackButton_Click" VerticalAlignment="Center"
                    PointerEntered="BackButton_PointerEntered" PointerExited="BackButton_PointerExited">
                <FontIcon x:Name="BackIcon" Glyph="&#xE72A;" FontSize="20">
                    <FontIcon.RenderTransform>
                        <ScaleTransform x:Name="BackIconScale" ScaleX="1" ScaleY="1"/>
                    </FontIcon.RenderTransform>
                </FontIcon>
            </Button>
            <TextBlock x:Name="PresetsTitle" Text="Presets" FontSize="24" FontWeight="Bold" Margin="16,0,16,0" VerticalAlignment="Center"/>
        </StackPanel>
        
        <!-- Content from PresetsPage -->
        <Grid Grid.Row="1" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="32" />
                <ColumnDefinition Width="320" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" VerticalAlignment="Stretch">
                <StackPanel Spacing="16">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,0">
                        <ComboBox x:Name="PresetComboBox" PlaceholderText="Select preset" Width="240" SelectionChanged="PresetComboBox_SelectionChanged"/>
                    </StackPanel>
                    <Button x:Name="LoadPresetButton" Click="LoadPresetButton_Click" Width="240" Margin="0,0,0,24">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE898;" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock x:Name="LoadPresetButtonText" Text="Load selected preset" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center" Margin="0,0,0,0">
                        <TextBox x:Name="PresetNameTextBox" Width="240" PlaceholderText="New preset name" SelectionHighlightColor="{ThemeResource SystemControlHighlightAccentBrush}"/>
                    </StackPanel>
                    <Button x:Name="SavePresetButton" Click="SavePresetButton_Click" Width="240" Margin="0,0,0,32">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE896;" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock x:Name="SavePresetButtonText" Text="Save as preset" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="DeletePresetButton" Click="DeletePresetButton_Click" Width="240" Margin="0,0,0,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE74D;" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock x:Name="DeletePresetButtonText" Text="Delete selected preset" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </ScrollViewer>
            <Border Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" VerticalAlignment="Stretch" HorizontalAlignment="Left" Margin="0,0,0,12"
                    Background="{ThemeResource ButtonBackground}" CornerRadius="8" BorderBrush="{ThemeResource ButtonBorderBrush}" BorderThickness="1">
                <ListView x:Name="PresetModsListView" Width="320" MaxWidth="320" VerticalAlignment="Stretch" />
            </Border>
        </Grid>
    </Grid>
</UserControl>
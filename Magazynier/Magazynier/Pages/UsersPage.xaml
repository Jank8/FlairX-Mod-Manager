<Page
    x:Class="Magazynier.Pages.UsersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="PageTitle" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button x:Name="AddButton" Grid.Column="1" Style="{StaticResource AccentButtonStyle}"
                    Click="AddButton_Click" Height="36">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;" FontSize="14"/>
                    <TextBlock x:Name="AddButtonText"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Search -->
        <AutoSuggestBox x:Name="SearchBox"
                        Grid.Row="1"
                        QueryIcon="Find"
                        Margin="0,0,0,12"
                        TextChanged="SearchBox_TextChanged"
                        QuerySubmitted="SearchBox_QuerySubmitted"/>

        <!-- Users List -->
        <Border Grid.Row="2"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="8">
            <Grid>
                <ListView x:Name="UsersList" SelectionMode="None">
                    <ListView.Header>
                        <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="0,0,0,1" Padding="16,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="80"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="ColName" Grid.Column="0" FontWeight="SemiBold" FontSize="12" Opacity="0.7"/>
                                <TextBlock x:Name="ColDept" Grid.Column="1" FontWeight="SemiBold" FontSize="12" Opacity="0.7"/>
                                <TextBlock x:Name="ColEmail" Grid.Column="2" FontWeight="SemiBold" FontSize="12" Opacity="0.7"/>
                                <TextBlock x:Name="ColPhone" Grid.Column="3" FontWeight="SemiBold" FontSize="12" Opacity="0.7"/>
                                <TextBlock x:Name="ColActive" Grid.Column="4" FontWeight="SemiBold" FontSize="12" Opacity="0.7"/>
                                <TextBlock Grid.Column="5" FontWeight="SemiBold" FontSize="12" Opacity="0.7"/>
                            </Grid>
                        </Border>
                    </ListView.Header>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="16,8" MinHeight="44">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="80"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding FullName}"
                                           FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding Department}"
                                           FontSize="12" VerticalAlignment="Center" Opacity="0.8"/>
                                <TextBlock Grid.Column="2" Text="{Binding Email}"
                                           FontSize="12" VerticalAlignment="Center" Opacity="0.8"/>
                                <TextBlock Grid.Column="3" Text="{Binding Phone}"
                                           FontSize="12" VerticalAlignment="Center" Opacity="0.8"/>
                                <FontIcon Grid.Column="4"
                                          Glyph="{Binding ActiveGlyph}"
                                          FontSize="16"
                                          Foreground="{Binding ActiveBrush}"
                                          VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="5" Orientation="Horizontal"
                                            Spacing="4" VerticalAlignment="Center">
                                    <Button Tag="{Binding}" Width="32" Height="32" Padding="0"
                                            Click="EditUser_Click" ToolTipService.ToolTip="Edit">
                                        <FontIcon Glyph="&#xE70F;" FontSize="13"/>
                                    </Button>
                                    <Button Tag="{Binding}" Width="32" Height="32" Padding="0"
                                            Click="DeleteUser_Click" ToolTipService.ToolTip="Delete">
                                        <FontIcon Glyph="&#xE74D;" FontSize="13"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>

                <StackPanel x:Name="EmptyState" Visibility="Collapsed"
                            HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
                    <FontIcon Glyph="&#xE716;" FontSize="48" Opacity="0.3"/>
                    <TextBlock x:Name="EmptyStateText" Opacity="0.5" FontSize="14" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
